The supplied image is a locally generated LLaDA-Image test input, not an upstream demo asset.

Upload `llada_image_edit_source.png` through the workflow's Load Image node before running either editing example. Installation does not write into ComfyUI's input directory.
